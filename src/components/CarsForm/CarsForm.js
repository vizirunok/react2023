import {useForm} from "react-hook-form";


const CarsForm = () => {

    const {handleSubmit, register} = useForm();

    return (
        <div>
            CarsForm
        </div>
    );
};

export {CarsForm};